<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EvenTopia">

        <!-- İlk açılan ekran (Launcher) -->
        <activity
            android:name="ui.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Etkinlik oluşturma ekranı -->
        <activity
            android:name="ui.CreateEventActivity"
            android:exported="false" />

        <!-- Detay ekranı -->
        <activity
            android:name="ui.EventDetailActivity"
            android:exported="false" />

        <!-- Liste ekranı -->
        <activity
            android:name="ui.EventListActivity"
            android:exported="false" />

        <!-- Eski MainActivity dursun ama launcher değil -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />
        <activity android:name="ui.TicketQrActivity" />

    </application>

</manifest>
